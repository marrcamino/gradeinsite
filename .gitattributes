# Normalise line endings so Windows and CI agree.
* text=auto eol=lf

# Windows-only tooling expects CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Never touch binaries.
*.png binary
*.ico binary
*.icns binary
*.sqlite binary
